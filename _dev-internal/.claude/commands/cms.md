---
description: Start the Strapi CMS dev server and report the URL
---

Run `npm run develop` in the background from the `cms/` directory, poll `http://localhost:1337/admin` until it returns HTTP 200, then tell me the local admin URL. If it's already running, just report the URL.
