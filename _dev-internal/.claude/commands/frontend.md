---
description: Start the Next.js dev server and report the URL
---

Run `npm run dev` in the background from the repo root, poll `http://localhost:3000` until it returns HTTP 200, then tell me the local URL. If it's already running, just report the URL.
